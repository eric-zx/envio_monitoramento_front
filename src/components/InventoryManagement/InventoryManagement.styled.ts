import styled from 'styled-components';

export const InventoryManagementWrapper = styled.div`
  padding: 40px 20px;
  max-width: 1000px;
  margin: 0 auto;
  font-family: 'Segoe UI', Arial, sans-serif;
  background-color: #f8f9fa;
  min-height: 100vh;
`;